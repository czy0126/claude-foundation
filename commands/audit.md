---
name: audit
description: 对当前变更运行完整性审计——检查文件存在性、日期一致性、测试结果
---

# /audit — 运行完整性审计

请对当前会话中的所有变更执行以下检查：

## 1. 文件存在性
列出所有被修改、创建和删除的文件：
- 修改的文件：[用 `git diff --name-only`]
- 每个预期输出文件是否都存在？

## 2. 测试结果
```bash
[运行项目测试命令]
```
- 全部通过？通过数/失败数/跳过数
- 如果有失败：列出失败的测试名称和关键错误信息

## 3. 一致性
- 所有生成的文件日期是否一致？
- 没有混入过期残留文件？

## 4. 总结
- [ ] 所有文件存在且一致
- [ ] 测试全部通过
- [ ] git diff 无意外变更
- [ ] CLAUDE.md 当前状态已更新

输出审计结果：`AUDIT_PASSED` / `AUDIT_WARNINGS` / `AUDIT_FAILED`
