---
paths:
  - "**/*.py"
---

# 测试驱动规则

## 在实施之前
1. 先写一个会失败的测试
2. 验证测试确实失败（不是因错误原因失败）
3. 然后实施最小实现使测试通过

## 测试应该覆盖
- 正常路径（happy path）
- 边界值（空输入、0、最大/最小值）
- 错误路径（无效输入→预期异常）

## 不要
- 跳过测试"先写功能"
- 写测试只测 mock（测试应该验证行为，不是验证 mock）
- 让测试依赖于外部状态（数据库、网络、文件系统）——用 fixtures 或 mock
